services:
  notifier:
    build: .
    container_name: incident-notifier
    restart: unless-stopped
    ports:
      - "5080:5080"
    volumes:
      - ./config:/config:ro
      - ./data:/data
    env_file:
      - ./config/secrets.env

